# MCP
MCP servers expose repository, GitHub and documentation operations through typed schemas and explicit permission labels. The in-process registry demonstrates the tool contract without arbitrary shell access. Production can run these as separate MCP processes while retaining identical permission checks.
