# Deployment
Use Docker Compose locally. For production, replace the development JSON store with SQLAlchemy/Postgres repositories, enable a real Qdrant adapter, configure Redis-backed ARQ workers, use managed secrets, TLS ingress, organization RBAC, OpenTelemetry exporters, and least-privilege GitHub App credentials.
